package ccamp.render.renderinfo;

import javafx.scene.paint.Color;

public class RawRenderInfo extends RenderInfo {

    private Color color;

    public RawRenderInfo(int x, int y) {
        super(x, y);
    }
}
